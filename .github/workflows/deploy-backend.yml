name: Deploy Backend to EC2

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch: # 수동 실행 가능

jobs:
  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        working-directory: ./backend
        run: npm ci

      - name: Build application
        working-directory: ./backend
        run: npm run build

      - name: Create deployment package
        working-directory: ./backend
        run: |
          mkdir -p deploy-package
          cp -r dist deploy-package/
          cp package.json package-lock.json ecosystem.config.js deploy-package/
          # package-lock.json이 없으면 생성
          if [ ! -f package-lock.json ]; then
            echo "⚠️ package-lock.json이 없습니다. 생성 중..."
            npm install --package-lock-only
          fi
          tar -czf deploy.tar.gz -C deploy-package .

      - name: Configure SSH
        env:
          EC2_HOST: ${{ secrets.EC2_HOST }}
          EC2_USER: ${{ secrets.EC2_USER }}
          EC2_SSH_KEY: ${{ secrets.EC2_SSH_KEY }}
        run: |
          # 환경 변수 확인
          echo "🔍 환경 변수 확인:"
          echo "EC2_HOST: ${EC2_HOST:+설정됨 (길이: ${#EC2_HOST})}${EC2_HOST:-❌ 설정되지 않음}"
          echo "EC2_USER: ${EC2_USER:+설정됨 (값: '$EC2_USER')}${EC2_USER:-❌ 설정되지 않음}"
          echo "EC2_SSH_KEY: ${EC2_SSH_KEY:+설정됨 (길이: ${#EC2_SSH_KEY})}${EC2_SSH_KEY:-❌ 설정되지 않음}"
          
          # 필수 변수 확인 (빈 문자열도 체크)
          MISSING_SECRETS=()
          if [ -z "${EC2_HOST:-}" ] || [ "$EC2_HOST" = "" ]; then
            MISSING_SECRETS+=("EC2_HOST")
          fi
          if [ -z "${EC2_USER:-}" ] || [ "$EC2_USER" = "" ]; then
            MISSING_SECRETS+=("EC2_USER")
          fi
          if [ -z "${EC2_SSH_KEY:-}" ] || [ "$EC2_SSH_KEY" = "" ]; then
            MISSING_SECRETS+=("EC2_SSH_KEY")
          fi
          
          if [ ${#MISSING_SECRETS[@]} -gt 0 ]; then
            echo "❌ 필수 Secrets가 설정되지 않았습니다!"
            echo "누락된 Secrets:"
            for secret in "${MISSING_SECRETS[@]}"; do
              echo "  - $secret"
            done
            echo ""
            echo "설정 방법:"
            echo "1. GitHub 저장소 → Settings → Secrets and variables → Actions"
            echo "2. New repository secret 클릭"
            echo "3. Name과 Secret 값 입력"
            echo ""
            echo "자세한 내용: docs/GITHUB_SECRETS_CHECKLIST.md"
            exit 1
          fi
          
          echo "✅ 모든 필수 Secrets가 설정되었습니다!"
          
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          
          # SSH 키 저장 (줄바꿈 보존)
          echo "$EC2_SSH_KEY" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          
          # SSH 키 형식 확인
          echo "🔑 SSH 키 형식 확인:"
          head -1 ~/.ssh/deploy_key
          tail -1 ~/.ssh/deploy_key
          echo "키 파일 크기: $(wc -c < ~/.ssh/deploy_key) bytes"
          
          # EC2 호스트 키 추가
          echo "🔐 EC2 호스트 키 추가 중..."
          ssh-keyscan -H "$EC2_HOST" >> ~/.ssh/known_hosts 2>&1 || true
          chmod 644 ~/.ssh/known_hosts
          
          # SSH 연결 테스트
          echo "🔍 SSH 연결 테스트 중..."
          ssh -i ~/.ssh/deploy_key \
              -o StrictHostKeyChecking=no \
              -o ConnectTimeout=10 \
              -v \
              "$EC2_USER@$EC2_HOST" \
              "echo 'SSH 연결 성공!'" || {
            echo "⚠️ SSH 연결 테스트 실패 (이 단계는 계속 진행됩니다)"
          }

      - name: Deploy to EC2
        env:
          EC2_HOST: ${{ secrets.EC2_HOST }}
          EC2_USER: ${{ secrets.EC2_USER }}
        run: |
          # 환경 변수 확인
          if [ -z "${EC2_HOST:-}" ] || [ -z "${EC2_USER:-}" ]; then
            echo "❌ EC2_HOST 또는 EC2_USER가 설정되지 않았습니다!"
            echo "EC2_HOST: ${EC2_HOST:-❌ 없음}"
            echo "EC2_USER: ${EC2_USER:-❌ 없음}"
            exit 1
          fi
          
          echo "🔍 배포 대상: $EC2_USER@$EC2_HOST"
          
          # SSH 연결 테스트
          echo "🔍 SSH 연결 테스트 중..."
          if ssh -i ~/.ssh/deploy_key \
              -o StrictHostKeyChecking=no \
              -o ConnectTimeout=10 \
              -o BatchMode=yes \
              "$EC2_USER@$EC2_HOST" \
              "echo 'SSH 연결 성공!'" 2>&1; then
            echo "✅ SSH 연결 성공!"
          else
            echo "❌ SSH 연결 실패!"
            echo "EC2_SSH_KEY Secret을 확인하세요."
            echo "자세한 내용: docs/SSH_KEY_TROUBLESHOOTING.md"
            exit 1
          fi
          
          # 배포 파일 전송
          echo "📤 배포 파일 전송 중..."
          scp -i ~/.ssh/deploy_key \
              -o StrictHostKeyChecking=no \
              -o ConnectTimeout=10 \
              backend/deploy.tar.gz \
              "$EC2_USER@$EC2_HOST:/tmp/"

          # EC2에서 배포 실행
          ssh -i ~/.ssh/deploy_key \
              -o StrictHostKeyChecking=no \
              "$EC2_USER@$EC2_HOST" << 'ENDSSH'
            set -e
            
            BACKEND_DIR="/home/ubuntu/chalog-backend"
            
            # curl 설치 확인
            if ! command -v curl &> /dev/null; then
              echo "📦 curl 설치 중..."
              sudo apt-get update
              sudo apt-get install -y curl
            fi
            
            # Node.js 설치 확인 및 설치
            if ! command -v node &> /dev/null || ! command -v npm &> /dev/null; then
              echo "📦 Node.js/npm이 설치되지 않았습니다. 설치 중..."
              curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
              sudo apt-get install -y nodejs
              
              # 명령어 캐시 새로고침
              hash -r
              
              # PATH에 Node.js 경로 추가
              export PATH="/usr/bin:/usr/local/bin:$PATH"
              
              # 설치 확인 (전체 경로로도 시도)
              if command -v node &> /dev/null || [ -f /usr/bin/node ]; then
                NODE_CMD=$(command -v node || echo "/usr/bin/node")
                NPM_CMD=$(command -v npm || echo "/usr/bin/npm")
                
                if [ -f "$NODE_CMD" ] && [ -f "$NPM_CMD" ]; then
                  echo "✅ Node.js 설치 완료: $($NODE_CMD --version)"
                  echo "✅ npm 설치 완료: $($NPM_CMD --version)"
                  # 심볼릭 링크 생성 (필요시)
                  [ ! -f /usr/local/bin/node ] && sudo ln -sf "$NODE_CMD" /usr/local/bin/node || true
                  [ ! -f /usr/local/bin/npm ] && sudo ln -sf "$NPM_CMD" /usr/local/bin/npm || true
                else
                  echo "❌ Node.js/npm 설치 실패!"
                  echo "Node.js 경로: $NODE_CMD"
                  echo "npm 경로: $NPM_CMD"
                  exit 1
                fi
              else
                echo "❌ Node.js/npm 설치 실패!"
                exit 1
              fi
            else
              echo "✅ Node.js 이미 설치됨: $(node --version)"
              echo "✅ npm 이미 설치됨: $(npm --version)"
            fi
            
            # npm 경로 확인
            echo "🔍 npm 경로: $(which npm || echo '/usr/bin/npm')"
            echo "🔍 node 경로: $(which node || echo '/usr/bin/node')"
            
            # 디렉토리 생성
            mkdir -p $BACKEND_DIR
            cd $BACKEND_DIR
            
            # 기존 파일 백업
            if [ -d "dist" ]; then
              echo "💾 기존 파일 백업 중..."
              BACKUP_DIR="/home/ubuntu/backups"
              mkdir -p $BACKUP_DIR
              tar -czf $BACKUP_DIR/backup-$(date +%Y%m%d-%H%M%S).tar.gz \
                  dist/ package.json package-lock.json ecosystem.config.js 2>/dev/null || true
            fi
            
            # 배포 파일 압축 해제
            echo "📦 배포 파일 압축 해제 중..."
            tar -xzf /tmp/deploy.tar.gz -C $BACKEND_DIR
            
            # 의존성 설치
            echo "📥 의존성 설치 중..."
            # npm 경로 확인 후 사용
            NPM_CMD=$(command -v npm || echo "/usr/bin/npm")
            if [ ! -f "$NPM_CMD" ]; then
              echo "❌ npm을 찾을 수 없습니다: $NPM_CMD"
              exit 1
            fi
            
            # 기존 node_modules 정리 (필요시)
            if [ -d "node_modules" ]; then
              echo "🧹 기존 node_modules 정리 중..."
              rm -rf node_modules
            fi
            
            # package-lock.json 확인
            if [ -f "package-lock.json" ]; then
              echo "📦 package-lock.json 발견. npm ci 시도..."
              if $NPM_CMD ci --production --legacy-peer-deps 2>&1; then
                echo "✅ npm ci 성공"
              else
                echo "⚠️ npm ci 실패. npm install로 대체..."
                # --production 없이 설치 후 node_modules 정리
                $NPM_CMD install --legacy-peer-deps --no-audit --no-fund
                # devDependencies 제거
                echo "🧹 devDependencies 제거 중..."
                $NPM_CMD prune --production
              fi
            else
              echo "📦 package-lock.json 없음. npm install 실행..."
              $NPM_CMD install --legacy-peer-deps --no-audit --no-fund
              # devDependencies 제거
              echo "🧹 devDependencies 제거 중..."
              $NPM_CMD prune --production
            fi
            
            # class-transformer 명시적 설치 (peer dependency 문제 해결)
            echo "📦 class-transformer 명시적 설치 중..."
            $NPM_CMD install class-transformer class-validator --save --legacy-peer-deps --no-audit --no-fund || true
            
            # 설치 확인 및 누락된 패키지 재설치
            echo "🔍 설치된 패키지 확인:"
            if [ -d "node_modules" ]; then
              echo "✅ node_modules 디렉토리 존재"
              echo "📊 node_modules 크기: $(du -sh node_modules | cut -f1)"
              
              # 필수 패키지 확인
              MISSING_PACKAGES=()
              REQUIRED_PACKAGES=("class-transformer" "class-validator" "@nestjs/common" "@nestjs/core" "mysql2")
              
              for pkg in "${REQUIRED_PACKAGES[@]}"; do
                if [ -d "node_modules/$pkg" ]; then
                  echo "✅ $pkg 설치됨"
                else
                  echo "❌ $pkg 누락됨!"
                  MISSING_PACKAGES+=("$pkg")
                fi
              done
              
              # 누락된 패키지가 있으면 재설치
              if [ ${#MISSING_PACKAGES[@]} -gt 0 ]; then
                echo "⚠️ 누락된 패키지 발견. 재설치 중..."
                for pkg in "${MISSING_PACKAGES[@]}"; do
                  echo "📦 $pkg 설치 중..."
                  $NPM_CMD install "$pkg" --production --legacy-peer-deps --no-audit --no-fund
                done
              fi
            else
              echo "❌ node_modules 디렉토리가 없습니다!"
              echo "전체 재설치 시도..."
              $NPM_CMD install --production --legacy-peer-deps --no-audit --no-fund
              if [ ! -d "node_modules" ]; then
                echo "❌ 의존성 설치 실패!"
                exit 1
              fi
            fi
            
            # 최종 확인
            echo "🔍 최종 패키지 확인:"
            for pkg in "${REQUIRED_PACKAGES[@]}"; do
              if [ -d "node_modules/$pkg" ]; then
                echo "✅ $pkg 확인됨"
              else
                echo "❌ $pkg 여전히 누락됨!"
                exit 1
              fi
            done
            
            # PM2 설치 확인 및 설치
            NPM_CMD=$(command -v npm || echo "/usr/bin/npm")
            if ! command -v pm2 &> /dev/null; then
              echo "📦 PM2가 설치되지 않았습니다. 설치 중..."
              sudo $NPM_CMD install -g pm2
              hash -r
              PM2_CMD=$(command -v pm2 || echo "/usr/local/bin/pm2")
              if [ -f "$PM2_CMD" ]; then
                echo "✅ PM2 설치 완료: $($PM2_CMD --version)"
                
                # PM2 부팅 시 자동 시작 설정
                echo "⚙️ PM2 부팅 시 자동 시작 설정 중..."
                $PM2_CMD startup systemd -u ubuntu --hp /home/ubuntu || echo "PM2 startup 설정은 수동으로 실행해야 할 수 있습니다."
              else
                echo "⚠️ PM2 설치 후 경로를 찾을 수 없습니다."
                exit 1
              fi
            else
              PM2_CMD=$(command -v pm2)
              echo "✅ PM2 이미 설치됨: $($PM2_CMD --version)"
            fi
            
            # PM2로 재시작
            echo "🔄 PM2로 앱 재시작 중..."
            $PM2_CMD delete chalog-backend 2>/dev/null || true
            $PM2_CMD start ecosystem.config.js
            $PM2_CMD save
            
            # Health check
            echo "🏥 Health check..."
            sleep 5
            PM2_CMD=$(command -v pm2 || echo "/usr/local/bin/pm2")
            if curl -f http://localhost:3000/health > /dev/null 2>&1; then
              echo "✅ 배포 성공! Health check 통과"
            else
              echo "⚠️ Health check 실패. 로그 확인:"
              $PM2_CMD logs chalog-backend --lines 20 --nostream || true
              echo "PM2 상태:"
              $PM2_CMD status || true
              exit 1
            fi
            
            # 정리
            rm -f /tmp/deploy.tar.gz
          ENDSSH

      - name: Cleanup
        if: always()
        run: |
          rm -f ~/.ssh/deploy_key
          rm -rf backend/deploy-package backend/deploy.tar.gz

      - name: Deployment Summary
        if: success()
        run: |
          echo "✅ 배포 완료!"
          echo "🌐 Backend URL: http://${{ secrets.EC2_HOST }}:3000"
          echo "🏥 Health Check: http://${{ secrets.EC2_HOST }}:3000/health"

