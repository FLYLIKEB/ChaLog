# Test Database Configuration
TEST_DATABASE_URL=mysql://admin:az980831@localhost:3307/chalog_test
# Set NODE_ENV to 'test' for test-specific configurations
NODE_ENV=test
# Disable DB synchronization for tests (managed manually)
DB_SYNCHRONIZE=false
# Optional: Enable SSL for test DB if needed (e.g., for cloud test DBs)
DB_SSL_ENABLED=false
DB_SSL_REJECT_UNAUTHORIZED=false
