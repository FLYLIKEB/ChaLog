# ============================================
# ChaLog Backend 환경 변수 설정
# ============================================
# 사용법: 이 파일을 .env로 복사하고 실제 값으로 수정하세요
# cp .env.example .env

# ============================================
# 서버 설정
# ============================================
NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:5173

# ============================================
# 데이터베이스 설정
# ============================================
# 로컬 MySQL 사용 시:
# DATABASE_URL=mysql://root:password@localhost:3306/chalog

# AWS RDS/Aurora 사용 시 (SSH 터널):
# SSH 터널을 통해 localhost:3307로 연결
DATABASE_URL=mysql://admin:password@localhost:3307/chalog

# 개발 환경에서만 true로 설정 (데이터 손실 위험 있음)
DB_SYNCHRONIZE=false

# AWS RDS/Aurora 사용 시 SSL 연결 활성화
DB_SSL_ENABLED=false
DB_SSL_REJECT_UNAUTHORIZED=true

# ============================================
# JWT 인증 설정
# ============================================
# 프로덕션에서는 반드시 강력한 비밀키로 변경하세요
# 생성 방법: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# ============================================
# SSH 터널 설정 (AWS RDS/Aurora 사용 시)
# ============================================
# SSH 키 파일 경로 (~ 사용 가능)
SSH_KEY_PATH=~/.ssh/your-key.pem

# EC2 인스턴스 정보
EC2_HOST=your-ec2-instance.compute.amazonaws.com
EC2_USER=ec2-user

# SSH 터널 포트 설정
SSH_TUNNEL_LOCAL_PORT=3307
SSH_TUNNEL_REMOTE_HOST=your-rds-endpoint.region.rds.amazonaws.com
SSH_TUNNEL_REMOTE_PORT=3306
