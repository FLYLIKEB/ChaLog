# ============================================
# ChaLog 프론트엔드 환경 변수
# ============================================
# 
# 사용 방법:
# 1. 이 파일을 복사하여 .env 파일 생성: cp .env.example .env
# 2. 실제 값으로 변경
# 3. .env 파일은 .gitignore에 포함되어 있어 커밋되지 않습니다
#
# 자세한 내용: docs/ENVIRONMENT_VARIABLES.md

# ============================================
# 백엔드 API 설정
# ============================================

# 백엔드 API 서버 URL
# 필수: ✅
# 설명: 프론트엔드가 요청을 보낼 백엔드 API 서버의 URL
# 개발 환경: http://localhost:3000
# 프로덕션: Vercel 환경 변수에서 설정 (예: http://your-ec2-ip:3000)
# 참고: 프로덕션에서는 반드시 환경 변수로 설정해야 합니다
VITE_API_BASE_URL=http://localhost:3000

# ============================================
# Serverless Proxy 설정 (Vercel API 프록시)
# ============================================

# Backend URL (Serverless Function이 프록시할 대상)
# 필수: ✅ (Vercel 환경 변수에서 BACKEND_URL로 설정)
# 설명: API 프록시가 연결할 백엔드 서버 URL
# 개발 환경에서는 .env에 설정해두면 로컬 vercel dev 실행 시 사용됩니다
# BACKEND_URL=http://localhost:3000

# Backend 요청 타임아웃 (밀리초)
# 기본값: 10000 (10초)
# 필요 시 조정: BACKEND_TIMEOUT_MS=15000

# 프록시 요청 로깅 (기본 true)
# LOG_PROXY_REQUESTS=true


# ============================================
# 카카오 로그인 설정
# ============================================

# 카카오 JavaScript SDK 앱 키
# 필수: ✅ (카카오 로그인 사용 시)
# 설명: 카카오 개발자 콘솔에서 발급받은 JavaScript 키
# 발급 위치: https://developers.kakao.com/ → 앱 설정 → 앱 키
# ⚠️ 주의: 프로덕션에서는 실제 앱 키로 변경하세요
# ⚠️ 보안: 이 값은 공개되어도 되지만, 앱 키 변경 시 즉시 업데이트하세요
# JavaScript 키 사용 (REST API 키가 아님!)
VITE_KAKAO_APP_KEY=a5654b03ecef8d26990a2f1fcb26fc05
